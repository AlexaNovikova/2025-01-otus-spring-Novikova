server:
  port: 8189

spring:
  data:
    mongodb:
      port: 0
      database: library_test

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2

mongock:
  enabled: false

logging:
  level:
    root: ERROR